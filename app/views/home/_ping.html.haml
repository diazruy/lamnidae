.row
  .span10.offset1
    .contentsection
      %p Have an awesome idea? Need some extra brainpower? Drop me a line.
      %p
      = simple_form_for ContactForm.new, remote: true, html: {class: 'contactform form-horizontal'} do |f|
        .alert.alert-success.success.hide
        .alert.alert-error.errors.hide
        = f.input :name
        = f.input :email
        = f.input :message, as: :text
        = f.input :nickname, wrapper_html: {class: 'hide'}
        .form-actions
          = f.button :submit, value: "Send", class: 'btn btn-primary'

